spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bobogit/config-repo
#          basedir:
#          username:
#          password:
  rabbitmq:
    host: 192.168.100.100
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"


